List.concat.assoc<A: Type>(
  xs: List<A>,
  ys: List<A>,
  zs: List<A>
): (xs ++ ys ++ zs) == (xs ++ ys) ++ zs
  case xs {
    nil: refl
    cons: let ind = List.concat.assoc<A>(xs.tail, ys, zs)
          let qed = apply(List.cons<A>(xs.head), ind)
          qed
  }!
