// Slice `str` from unsigned index `a` (inclusive) to `b` (exclusive).
Word.slice<size: Nat>(a: Word(size), b: Word(size), str: String): String
  String.slice(Word.to_nat<size>(a), Word.to_nat<size>(b), str)
