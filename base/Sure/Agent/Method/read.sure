Sure.Agent.Method.read(s: String): Maybe<Sure.Agent.Method>
  if String.eql(s, "sure.parse") then some(Sure.Agent.Method.parse)
  else if String.eql(s, "sure.check") then some(Sure.Agent.Method.check)
  else if String.eql(s, "sure.normalize") then some(Sure.Agent.Method.normalize)
  else if String.eql(s, "sure.infer") then some(Sure.Agent.Method.infer)
  else if String.eql(s, "sure.goal") then some(Sure.Agent.Method.goal)
  else if String.eql(s, "sure.holes") then some(Sure.Agent.Method.holes)
  else if String.eql(s, "sure.symbols") then some(Sure.Agent.Method.symbols)
  else if String.eql(s, "sure.definition") then some(Sure.Agent.Method.definition)
  else if String.eql(s, "sure.references") then some(Sure.Agent.Method.references)
  else if String.eql(s, "sure.dependencies") then some(Sure.Agent.Method.dependencies)
  else if String.eql(s, "sure.patch") then some(Sure.Agent.Method.patch)
  else if String.eql(s, "sure.compile") then some(Sure.Agent.Method.compile)
  else if String.eql(s, "sure.diagnostics") then some(Sure.Agent.Method.diagnostics)
  else if String.eql(s, "sure.repair") then some(Sure.Agent.Method.repair)
  else if String.eql(s, "sure.prove") then some(Sure.Agent.Method.prove)
  else if String.eql(s, "sure.impact") then some(Sure.Agent.Method.impact)
  else if String.eql(s, "sure.theorems") then some(Sure.Agent.Method.theorems)
  else if String.eql(s, "sure.docs") then some(Sure.Agent.Method.docs)
  else if String.eql(s, "sure.graph") then some(Sure.Agent.Method.graph)
  else if String.eql(s, "sure.qc") then some(Sure.Agent.Method.qc)
  else if String.eql(s, "sure.fill") then some(Sure.Agent.Method.fill)
  else if String.eql(s, "sure.trace") then some(Sure.Agent.Method.trace)
  else if String.eql(s, "sure.gen") then some(Sure.Agent.Method.gen)
  else if String.eql(s, "sure.debug") then some(Sure.Agent.Method.debug)
  else if String.eql(s, "kind.parse") then some(Sure.Agent.Method.parse)
  else if String.eql(s, "kind.check") then some(Sure.Agent.Method.check)
  else if String.eql(s, "kind.normalize") then some(Sure.Agent.Method.normalize)
  else if String.eql(s, "kind.infer") then some(Sure.Agent.Method.infer)
  else if String.eql(s, "kind.goal") then some(Sure.Agent.Method.goal)
  else if String.eql(s, "kind.holes") then some(Sure.Agent.Method.holes)
  else if String.eql(s, "kind.symbols") then some(Sure.Agent.Method.symbols)
  else if String.eql(s, "kind.definition") then some(Sure.Agent.Method.definition)
  else if String.eql(s, "kind.references") then some(Sure.Agent.Method.references)
  else if String.eql(s, "kind.dependencies") then some(Sure.Agent.Method.dependencies)
  else if String.eql(s, "kind.patch") then some(Sure.Agent.Method.patch)
  else if String.eql(s, "kind.compile") then some(Sure.Agent.Method.compile)
  else if String.eql(s, "kind.diagnostics") then some(Sure.Agent.Method.diagnostics)
  else if String.eql(s, "kind.repair") then some(Sure.Agent.Method.repair)
  else if String.eql(s, "kind.prove") then some(Sure.Agent.Method.prove)
  else if String.eql(s, "kind.impact") then some(Sure.Agent.Method.impact)
  else if String.eql(s, "kind.theorems") then some(Sure.Agent.Method.theorems)
  else if String.eql(s, "kind.docs") then some(Sure.Agent.Method.docs)
  else if String.eql(s, "kind.graph") then some(Sure.Agent.Method.graph)
  else if String.eql(s, "kind.qc") then some(Sure.Agent.Method.qc)
  else if String.eql(s, "kind.fill") then some(Sure.Agent.Method.fill)
  else if String.eql(s, "kind.trace") then some(Sure.Agent.Method.trace)
  else if String.eql(s, "kind.gen") then some(Sure.Agent.Method.gen)
  else if String.eql(s, "kind.debug") then some(Sure.Agent.Method.debug)
  else none
