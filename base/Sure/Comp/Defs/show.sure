// Converts a defs to a readable string
Sure.Comp.Defs.show(defs: Sure.Comp.Defs): String
  let defs = Sure.Map.to_list!(defs)
  let text = ""
  let text = for defn in defs:
    let {name,term} = defn
    text | name | " = " | Sure.Comp.show(term) | "\n"
  text
