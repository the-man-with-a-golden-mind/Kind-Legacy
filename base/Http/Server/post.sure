Http.Server.post(pattern: String, req: Http.Server.Req): Maybe<List<Pair<String, String>>>
  open req
  if Bool.or(String.eql(req.method, "POST"), String.eql(req.method, "post")) then
    Http.Path.match(pattern, req.url)
  else
    none
