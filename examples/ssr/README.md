# sure/ssr

Server-rendered pages on `127.0.0.1:8771`. Tailwind + daisyUI. `<` is escaped.

```bash
sure prove
sure run
```

```bash
curl http://127.0.0.1:8771/
curl http://127.0.0.1:8771/about
curl http://127.0.0.1:8771/user/ada
```
